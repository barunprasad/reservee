{
  "name": "rides",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/rides/src",
  "projectType": "application",
  "tags": [],
  "targets": {
    "build": {
      "executor": "@nx/rspack:rspack",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/apps/rides",
        "index": "apps/rides/src/index.html",
        "baseHref": "/",
        "main": "apps/rides/src/main.ts",
        "tsConfig": "apps/rides/tsconfig.app.json",
        "assets": ["apps/rides/src/favicon.ico", "apps/rides/src/assets"],
        "rspackConfig": "apps/rides/rspack.config.ts",
        "styles": ["apps/rides/src/styles.scss"],
        "scripts": [],
        "configurations": {
          "development": {
            "mode": "development"
          },
          "production": {
            "mode": "production",
            "optimization": true,
            "sourceMap": false,
            "outputHashing": "all",
            "namedChunks": false,
            "extractLicenses": true,
            "vendorChunk": false
          }
        },
        "target": "web"
      },
      "configurations": {
        "production": {
          "rspackConfig": "apps/rides/rspack.config.prod.ts"
        }
      }
    },
    "serve": {
      "executor": "@nx/rspack:module-federation-dev-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "rides:build",
        "hmr": true,
        "port": 4205
      },
      "configurations": {
        "development": {
          "buildTarget": "rides:build:development"
        },
        "production": {
          "buildTarget": "rides:build:production",
          "hmr": false
        }
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "serve-static": {
      "executor": "@nx/rspack:module-federation-static-server",
      "defaultConfiguration": "production",
      "options": {
        "serveTarget": "rides:serve"
      },
      "configurations": {
        "development": {
          "serveTarget": "rides:serve:development"
        },
        "production": {
          "serveTarget": "rides:serve:production"
        }
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/rides/jest.config.ts"
      }
    }
  }
}
